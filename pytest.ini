[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra --strict-markers --cov=core --cov=exchange --cov=trading --cov-report=term-missing --cov-config=.coveragerc
markers =
    unit: Unit tests with no network/external APIs.
    integration: Integration tests that may call external services.
